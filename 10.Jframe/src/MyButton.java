import javax.swing.*;

public class MyButton extends JButton {

    public MyButton(String title, int width, int height, boolean isVisible){
        super(title);
        setSize(width, height);
    }
}
